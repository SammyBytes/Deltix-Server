# Context: addons

Status: placeholder — implementation scheduled for Fase 4 of the roadmap
(dynamic Add-on loading via `import()`, gated by the active license).

Only `index.ts` from this folder may be imported by other contexts (ACL boundary).
