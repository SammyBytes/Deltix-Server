# Context: transfer

Status: placeholder — implementation scheduled for Fase 3 of the roadmap
(Ephemeral Tickets & gRPC Engine on :50051).

Only `index.ts` from this folder may be imported by other contexts (ACL boundary).
