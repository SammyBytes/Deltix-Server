/**
 * Placeholder for the "transfer" bounded context.
 *
 * This is the ONLY file other contexts/modules are allowed to import from
 * (ACL boundary). Internals of this context must never be imported directly
 * from outside (e.g. `contexts/transfer/some-internal-file`).
 *
 * Implementation lands in Fase 3 of the roadmap (Ephemeral Tickets & gRPC
 * Engine on :50051). See README.md at the repo root and
 * .github/copilot-instructions.md for architecture rules.
 */
export {};
